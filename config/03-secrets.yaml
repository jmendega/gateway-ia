# ADVERTENCIA: Este archivo contiene placeholders.
# Copiar como 03-secrets.local.yaml, reemplazar los valores y NUNCA commitear el archivo local.
# En producción usar External Secrets Operator o HashiCorp Vault.
apiVersion: v1
kind: Secret
metadata:
  name: openai-api-key
  namespace: ai-gateway
type: Opaque
stringData:
  apiKey: "<OPENAI_API_KEY>"
---
apiVersion: v1
kind: Secret
metadata:
  name: anthropic-api-key
  namespace: ai-gateway
type: Opaque
stringData:
  apiKey: "<ANTHROPIC_API_KEY>"
